Passengers: <% @passengers.each do |passenger| %><br>
    <%= passenger.name %><br>
<% end %>
<%= form_for(@booking, :url => booking_path(@booking.id), :html => { :method => "get"}) do |f| %>
    Flight: <%= @flight_id %>
    <%= f.hidden_field :booking_id, value: @booking.id %>
    <%= f.submit "Confirm" %>
<% end %>
ID: <%= @booking.id %>
