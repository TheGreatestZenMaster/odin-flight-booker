<%= form_for(@booking, :url => bookings_path, :html => { :method => 'GET' }) do |f| %>
    <%= f.select :flight_id, @flight_options %>
    <%= f.hidden_field :passengers, value: @number_of_passengers %>
    <%= f.submit%>
<% end %>